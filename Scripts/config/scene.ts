module config {
    export enum Scene {
        START,
        PLAY,
        OVER,
        SCENE_COUNT
    }

    export const SCREEN_WIDTH:number = 640;
    export const SCREEN_HEIGHT:number = 480;
}